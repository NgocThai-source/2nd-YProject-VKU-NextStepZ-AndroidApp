package com.example.nextstepz.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Brand Gradient Colors (from logo) ───────────────────────────────────────
val BrandBlue = Color(0xFF2196F3)
val BrandCyan = Color(0xFF00BCD4)
val BrandMagenta = Color(0xFFE040FB)
val BrandPink = Color(0xFFFF4081)
val BrandPurple = Color(0xFF7C4DFF)

// ─── Primary Gradient Stops ──────────────────────────────────────────────────
val GradientStart = Color(0xFF1E88E5)       // Deep blue
val GradientMid = Color(0xFF7C4DFF)         // Purple
val GradientEnd = Color(0xFFE040FB)          // Magenta

// ─── Background Colors ──────────────────────────────────────────────────────
val DarkBackground = Color(0xFF0A0E1A)       // Very dark navy
val DarkSurface = Color(0xFF111827)           // Dark card surface
val DarkSurfaceVariant = Color(0xFF1A2035)    // Slightly lighter surface
val DarkCardBackground = Color(0xFF161B2E)    // Card/container background

// ─── Text Colors ────────────────────────────────────────────────────────────
val TextPrimary = Color(0xFFF1F5F9)           // Almost white
val TextSecondary = Color(0xFF94A3B8)          // Muted gray-blue
val TextTertiary = Color(0xFF64748B)           // Even more muted
val TextOnGradient = Color(0xFFFFFFFF)         // White text on gradient

// ─── Glassmorphism ──────────────────────────────────────────────────────────
val GlassWhite = Color(0x1AFFFFFF)            // 10% white
val GlassBorder = Color(0x33FFFFFF)           // 20% white border
val GlassOverlay = Color(0x0DFFFFFF)          // 5% white overlay

// ─── Status Colors ──────────────────────────────────────────────────────────
val SuccessGreen = Color(0xFF10B981)
val ErrorRed = Color(0xFFEF4444)
val WarningYellow = Color(0xFFF59E0B)
val InfoBlue = Color(0xFF3B82F6)

// ─── Input Field Colors ─────────────────────────────────────────────────────
val InputBackground = Color(0xFF1A2035)
val InputBorder = Color(0xFF2A3150)
val InputBorderFocused = Color(0xFF7C4DFF)
val InputPlaceholder = Color(0xFF64748B)

// ─── Social Login Colors ────────────────────────────────────────────────────
val GoogleRed = Color(0xFFEA4335)
val FacebookBlue = Color(0xFF1877F2)

// ─── Navigation Bar Colors ──────────────────────────────────────────────────
val NavBarBackground = Color(0xE6111827)        // DarkSurface 90% opacity
val NavBarIndicator = Color(0x1A7C4DFF)         // Purple 10% — active bg
val NavBarInactive = Color(0xFF64748B)          // Same as TextTertiary

// ─── Home Screen Accents ────────────────────────────────────────────────────
val AccentOrange = Color(0xFFFF6B35)            // Warm accent (from logo)
val AccentCyan = Color(0xFF06B6D4)              // Cool accent
val AccentEmerald = Color(0xFF10B981)           // Green accent
val AccentAmber = Color(0xFFF59E0B)             // Amber accent